services:
  api:
    image: example/api
    ports:
      - "8080:80"
  worker:
    image: example/worker
    environment:
      WORKER_PORT: "8080"
  cache:
    image: redis:7
    ports:
      - "6379:6379"
